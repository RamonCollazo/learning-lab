# On second terminal window

kubectl exec --stdin --tty test-pod -- bash

curl --silent --output /dev/null --show-error --max-time 5 --fail https://www.google.com

# Check logs on main terminal window
